name: Build Main Image

on:
  push:
    branches: [master]
    paths-ignore:
      - '*.md'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-healthcheck.yml'
  workflow_dispatch:
    inputs:
      platforms:
        description: 'Target platforms'
        required: false
        default: 'linux/amd64,linux/arm64,linux/arm/v7'
        type: string
      build_nightly:
        description: 'Build nightly version'
        required: false
        default: false
        type: boolean
      tag:
        description: 'Custom tag (optional)'
        required: false
        type: string

permissions:
  contents: read

jobs:
  build-main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Check Typescript
        run: npm run check-ts

      - name: Build
        run: npm run build:frontend

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Generate tags
        id: tags
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          CUSTOM_TAG: ${{ inputs.tag }}
          GIT_SHA: ${{ github.sha }}
        run: |
          TAGS="${DOCKER_USERNAME}/dockge:latest"

          # Add custom tag if provided
          if [ -n "${CUSTOM_TAG}" ]; then
            TAGS="${TAGS},${DOCKER_USERNAME}/dockge:${CUSTOM_TAG}"
          fi

          # Add git sha tag
          SHORT_SHA=$(echo "${GIT_SHA}" | cut -c1-7)
          TAGS="${TAGS},${DOCKER_USERNAME}/dockge:${SHORT_SHA}"

          # Add date tag
          DATE_TAG=$(date -u +%Y%m%d)
          TAGS="${TAGS},${DOCKER_USERNAME}/dockge:${DATE_TAG}"

          echo "tags=${TAGS}" >> $GITHUB_OUTPUT
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "date_tag=${DATE_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push Main Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          platforms: ${{ inputs.platforms || 'linux/amd64,linux/arm64,linux/arm/v7' }}
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/dockge:latest
          cache-to: type=inline
          target: release

      - name: Build and push Nightly Image
        if: ${{ github.event_name == 'push' || inputs.build_nightly }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          platforms: ${{ inputs.platforms || 'linux/amd64,linux/arm64,linux/arm/v7' }}
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/dockge:nightly
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/dockge:nightly
          cache-to: type=inline
          target: nightly

      - name: Summary
        run: |
          echo "✅ Main image built and pushed successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Built Images" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Release Images:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.tags.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

          if [ "${{ github.event_name }}" == "push" ] || [ "${{ inputs.build_nightly }}" == "true" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Nightly Image:**" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "${{ secrets.DOCKER_USERNAME }}/dockge:nightly" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🏗️ **Platforms:** ${{ inputs.platforms || 'linux/amd64,linux/arm64,linux/arm/v7' }}" >> $GITHUB_STEP_SUMMARY
          echo "📅 **Date:** $(date -u +%Y-%m-%d\ %H:%M:%S) UTC" >> $GITHUB_STEP_SUMMARY
          echo "🔖 **Commit:** ${{ steps.tags.outputs.short_sha }}" >> $GITHUB_STEP_SUMMARY

      - name: Docker Hub Description
        if: github.event_name == 'push'
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: ${{ secrets.DOCKER_USERNAME }}/dockge
          short-description: "Dockge - A fancy, easy-to-use and reactive docker compose.yaml manager"
          readme-filepath: ./README.md
        continue-on-error: true
